version: "3.9"

services:
  frontend:
    build:
      context: ./kezdesu-client
    ports:
      - "3000:3000"
    environment:
      - CHOKIDAR_USEPOLLING=true  # чтобы hot reload работал в контейнере
    stdin_open: true
    tty: true
